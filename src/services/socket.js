// var io = require('socket.io-client')
// var appConfig = require('webpack-config-loader!../config.js')
// var socket
// socket = io.connect(appConfig.messengerUrl, {
//   reconnect: true
// })

// require('./connect').apiCall('', '/connexion', 'GET', function (err, response) {
//   if (!err) {
//     socket.emit('authenticate', {
//       token: response.token
//     })
//   }
// })

// module.exports = socket
